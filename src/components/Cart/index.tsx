import React from 'react';
import * as Styled from './styled';

const Cart: React.FC = () => {
    return (
        <Styled.CartContainer>
            <p>Cart</p>
        </Styled.CartContainer>
    )
};

export default Cart;